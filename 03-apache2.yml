---
- name: update u5 servers
  hosts: u5
  become: yes

  tasks:
    - name: Install apache
      apt:
        name: apache2
        state: present

    - name: start and enable apache2
      service:
        name: apache2
        state: started
        enabled: yes
